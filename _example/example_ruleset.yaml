# Allowed scopes: output, buildtime
# Allowed categories: negative, positived
# Allowed target: command, os, fs

rules:
  - scope: output
    category: Negative
    instruction: dir_content_count('/var/lib/apt/lists/') > 0
    description: Apt lists should be cleared in final container
    id: apt-lists
    target: fs

